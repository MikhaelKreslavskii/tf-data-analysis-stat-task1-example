import pandas as pd
import numpy as np


chat_id = 613759661 # Ваш chat ID, не меняйте название переменной
def solution(x: np.array) -> float:
    t=10
    n=len(x)
    v=x
    a=2*((np.exp(len(x))+41)/100)
    return a # Ваш ответ
